import { BrowserRouter } from "react-router-dom";
import App from "./App";
import "./App.css";

const Providers = () => {
  return (
    <BrowserRouter>
      <App />
    </BrowserRouter>
  );
};

export default Providers;
